import { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
	appId: "gutembergue.app",
	appName: "twiter-clone",
	webDir: "www",
	bundledWebRuntime: false
}

export default config
