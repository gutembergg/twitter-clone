import { StickSegmentDirective } from './stick-segment.directive';

describe('StickSegmentDirective', () => {
  it('should create an instance', () => {
    const directive = new StickSegmentDirective();
    expect(directive).toBeTruthy();
  });
});
